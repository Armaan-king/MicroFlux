# Report — neural comparison, corrected, and replication (2026-09-13; updated 2026-09-15 with batch session 1)

Three bodies of evidence, kept apart. Every interval is a paired block
bootstrap on the validation (or test) window with the block count stated;
seed variability is the standard deviation of validation NLL across training
seeds and is a different quantity from sampling uncertainty. Neither is a
confidence interval across market sessions.

## 1. Superseded — the original pilot (2026-09-09 exploratory session)

Run with four implementation defects, since fixed with regression tests
(`tests/test_regressions.py`; ARCHITECTURE.md D13). Kept only as a record:
`docs/pilot-neural-2026-09-09-SUPERSEDED.log`. Its numbers (attention N = 64
+0.0164 ± 0.0084 over E1′, three seeds, patience 4) are replaced by §2.

## 2. Corrected — exploratory session `BTCUSDT-2026-09-09` (validation only)

8.00 h, 147,216 orders; train 5.60 h, validation 1.20 h (13,660 events).
Reference = E1′ (best classical on validation, −0.4435). Five seeds,
patience 8, epochs 10–40, N = 64. Artifacts: `runs/BTCUSDT-2026-09-09/`.

Blocks behind each interval: **72 (60 s) / 15 (300 s) / 5 (900 s)**.

| | val NLL, mean of 5 seeds | seed s.d. | gain vs E1′ (60 s) | 300 s | 900 s |
|---|---|---|---|---|---|
| MLP over summaries | −0.4395 | 0.0040 | −0.0040; per seed +0.001, −0.005, −0.003, −0.010, −0.004; 4/5 intervals include 0, 1/5 below | same sign | same sign |
| attention N = 64 | −0.4692 | 0.0029 | **+0.0257**; per seed +0.028, +0.021, +0.028, +0.025, +0.027; 5/5 intervals clear of 0 | 5/5 clear | 5/5 clear |
| attention − MLP, paired within seed | | 0.0031 | **+0.0297**; 5/5 clear | 5/5 clear | 5/5 clear |

Validation KS (jittered): E1′ 0.039 / 0.044 → attention 0.012–0.017 / 0.022–0.031.
Runtime per attention seed 23–78 min on CPU; MLP 1–2 min.

Reading: on this session the seed spread of the pilot was the stopping rule —
with patience 8 the five attention seeds land within 0.003 of each other. The
MLP over these summaries does not improve on E1′; that is a statement about
this control, not about whether activity summaries carry information.

## 3. Fresh session 1 — `BTCUSDT-2026-09-09-early`

2.00 h (05:04–07:04 UTC, no overlap with §2), 22,134 orders; train 1.40 h,
validation 0.30 h (≈ 3,300 events), test 0.30 h (2,779 events).
Artifacts: `runs/BTCUSDT-2026-09-09-early/`.

Blocks behind each interval: **18 (60 s) / 4 (300 s) / 2 (900 s)**. The 300 s
and 900 s intervals are **insufficient for inference** on windows this short
(PROTOCOL.md §7) and are reported for completeness only.

**Classical replication** (`replication.md`): H1 — marks — **confirmed**,
+0.1416 [+0.126, +0.158] at 60 s, control −0.0006 ± 0.0014. The ladder's
spine replicates (Hawkes over Poisson +0.78; M4 over M1 +0.14). Not confirmed
as registered: H2 (direction holds — 1-fill fast share 0.26/0.29, 20+
0.91/0.85 — strict monotonicity fails on SELL), H3 (state in kernel
−0.0031 [−0.007, +0.002]), H4 (underpowered), H5 (BUY monotone; SELL
0.515/0.340/0.348), H6, H7, H8, H9. E1′ is *worse* than M4 here (−0.0010).
Reference for the neural stage = M4 (−0.0007). Rate over-prediction on
validation is present again (residual mean 1.17 vs null 1.03).

**Neural, validation:**

| | val NLL, mean of 5 | seed s.d. | gain vs M4 (60 s, 18 blocks) |
|---|---|---|---|
| MLP | −0.0072 | 0.0049 | +0.0064; 0/5 intervals clear of 0 |
| attention N = 64 | −0.0098 | 0.0036 | +0.0090; 0/5 clear |
| attention − MLP, paired | | 0.0065 | +0.0026; 4/5 positive, 0/5 clear |

**Final test, scored once** (`final_test.json`; M4 = 0.1356 NLL/event):

| | test gain vs M4 (60 s, 18 blocks) | per seed |
|---|---|---|
| MLP | −0.0140, seed s.d. 0.0030 | all five negative |
| attention | −0.0057, seed s.d. 0.0100 | +0.005, +0.003, −0.006, −0.018, −0.012 |

**Conclusion for this session: the neural advantage was not replicated here.**
Validation gains are within sampling uncertainty; on the untouched test
segment both neural models score below the classical reference. This is a
valid result. It does not show that summaries are useless, that attention is
ineffective, or that the amount of training data caused it; it shows that on
this session, under the frozen configuration, the classical reference was not
beaten.

## 4. Batch session 1 — `BTCUSDT-2026-09-14` (first 8-hour fresh session)

8.50 h (00:00–08:30 UTC, `docs/collection-batch-1.md`), 113,043 orders; train 5.95 h,
validation 1.27 h (13,576 events), test 1.27 h (17,413 events). Eligibility and batch
rules passed before fitting (`eligibility.json`, fingerprint `56f7336d685fc703`).
Artifacts: `runs/BTCUSDT-2026-09-14/`. Wall clock 4 h 13 min: replication 44 min,
neural 3 h 15 min, final test 2 min.

Blocks behind each interval, validation and test alike: **77 (60 s) / 16 (300 s) / 6 (900 s)**.

**Classical replication** (`replication.md`): **H1 confirmed** (marks +0.1120
[+0.103, +0.121] at 60 s, control −0.0007 ± 0.0007); **H2 confirmed** (fast share by
mark 0.20 / 0.51 / 0.86 / 1.00 BUY, monotone); **H5 confirmed** (μ BUY 0.39 / 0.57 / 0.74,
SELL 0.54 / 0.35 / 0.25); **H8 confirmed** (E2 +0.0008 [+0.0001, +0.0015]). Not confirmed
as registered: H3 (state in kernel +0.0023 [−0.0001, +0.0045] — the interval touches
zero), H4, H6, H7, H9. E1′ beats M4 by +0.0018 and is the reference (−0.2637 on
validation). Rate over-prediction on validation again (residual mean 1.10–1.12 for M4).

**Neural, validation:**

| | val NLL, mean of 5 | seed s.d. | gain vs E1′ (60 s) | 300 s | 900 s |
|---|---|---|---|---|---|
| MLP | −0.2713 | 0.0014 | +0.0076; per seed +0.007, +0.008, +0.010, +0.008, +0.006; 3/5 intervals clear of 0 | 4/5 clear | 3/5 clear |
| attention N = 64 | −0.2915 | 0.0046 | **+0.0278**; per seed +0.022, +0.031, +0.033, +0.024, +0.030; 5/5 clear | 5/5 clear | 5/5 clear |
| attention − MLP, paired within seed | | 0.0044 | **+0.0202**; 5/5 clear | 5/5 clear | 5/5 clear |

All five MLP seeds ran to the 40-epoch maximum; attention seeds stopped at 20–40.

**Final test, scored once** (`final_test.json`; E1′ = −0.4065 NLL/event, 17,413 events):

| | test gain vs E1′ (60 s, 77 blocks) | 300 s | 900 s | per seed |
|---|---|---|---|---|
| MLP | +0.0037, seed s.d. 0.0023, 0/5 clear | 0/5 | 0/5 | +0.006, +0.001, +0.006, +0.002, +0.004 |
| attention | **+0.0339**, seed s.d. 0.0049, **5/5 clear** | 5/5 | 5/5 | +0.028, +0.032, +0.040, +0.037, +0.031 |

**Conclusion for this session: the attention advantage over the classical reference
replicated, on validation and on the untouched test segment, every seed, every block
size.** The MLP over summaries is slightly ahead of E1′ on validation and not
distinguishable from it on test.

## Standing

| evidence | attention vs classical reference |
|---|---|
| exploratory session, validation (§2) | +0.026, 5/5 seeds clear of zero, 72 blocks |
| fresh session 1 (2 h), validation (§3) | +0.009, 0/5 clear, 18 blocks |
| fresh session 1 (2 h), test, once (§3) | −0.006, 3/5 seeds negative, 18 blocks |
| batch session 1 (8.5 h), validation (§4) | +0.028, 5/5 clear, 77 blocks |
| batch session 1 (8.5 h), test, once (§4) | **+0.034, 5/5 clear, 77 blocks** |

PROTOCOL.md §7 requires the direction on ≥ 2 fresh sessions with intervals
clear of zero. Two fresh sessions have been evaluated: the 2-hour session did
not meet it; the first 8-hour batch session did, on validation and on test.
The 2-hour negative result stays in the record.

**The batch conclusion is pending session 2** (`BTCUSDT-2026-09-15`, to be
captured and evaluated under the same frozen protocol, whatever it shows).
Until then the classical model remains the working reference. Continuous
marks, richer book inputs, book-history encoders and any tuning wait until
the batch is complete.
