# Handoff — 2026-09-15

## Completed and verified

Commits `2a0ed26` and `782a7fa` add the completed BTCUSDT-2026-09-14
artifacts, report, D15, protocol log, registry/batch entry, and notebook section 7.
They change no model implementation or training settings. All ten neural runs
and the one-time final test are complete. The notebook has 13 sequentially
executed code cells, zero saved error outputs, and valid notebook structure.

Attention's mean gain over E1′ is +0.0278 nats/event on validation and +0.0339
on test; all five seeds' primary 60 s intervals exclude zero. MLP gains are
+0.0076 and +0.0037; its test intervals all include zero. The earlier negative
2-hour session remains in the evidence. Batch 1 has one of two required long
sessions; the classical reference remains in place pending batch completion.

The reporting audit corrects the final-test duration to approximately 13 min,
records both reasons H3 fails, and qualifies H8's sensitivity. No saved model
or evaluation artifact was rewritten, and no final test was rescored.

## Next capture

As checked on September 15 after 18:26 UTC, no capture or training worker is
running, and no next capture is scheduled. The old task
`TickForge-Overnight-20260913` has no trigger; its supervisor status is complete.
The BTCUSDT data root contains UTC partitions 2026-09-09, 2026-09-13 and
2026-09-14, with none for 2026-09-15 or 2026-09-16.

The originally planned September 15 start window was missed. Record this as
an operational scheduling deviation; the prospective collection plan and its
scientific rules remain unchanged. The next feasible target is UTC date
**2026-09-16**: start between **8 p.m. EDT September 15 and 11 a.m. EDT
September 16**, following the existing conservative start window. The earliest
start would finish capture at approximately **4:30 a.m. EDT September 16**.

From the TickForge repository, at that time:

```powershell
Set-Location -LiteralPath 'C:\Users\iidab\OneDrive\Desktop\TickForge'
powershell -File scripts\run_capture.ps1 -Hours 8.5
```

Keep the capture window open, with mains power and network connectivity. Check
that the target UTC partition is still absent before starting. The existing
TickForge wrapper suppresses idle sleep and releases the request on exit.

After capture closes, from MicroFlux:

```powershell
Set-Location -LiteralPath 'C:\Users\iidab\OneDrive\Desktop\MicroFlux'
powershell -File scripts\run_detached.ps1 -Date 2026-09-16 -Batch docs/collection-batch-1.json
```

This runs eligibility/batch checks, classical replication, the frozen MLP and
attention comparison (N=64, seeds 0–4), and the final test once. Allow roughly
4–5 hours of compute based on session 1, subject to event count and stopping.
Monitor `runs/BTCUSDT-2026-09-16/run.log`, `run.log.stdout` and `run.err`.
The launcher previously recorded a blank exit code; confirm completion from
the final artifacts and registry entry, not merely the wrapper's exit status.

The runner records the accepted session's actual date in the registry and
batch JSON. It does not rewrite the prospective schedule; this note records
the date deviation explicitly. Afterward, report both batch sessions whatever
their results and apply the frozen decision rule before considering model
extensions. Diffusion remains a later, undecided research stage.
